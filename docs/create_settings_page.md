# Create Settings Page
Creates a settings page where users can edit their name and reset password. This page will also include a link
to a page to list locations and edit them.

## Execution
Execute necessary backend and frontend changes to implement the settings page. The page should have a section 
to edit the user's name with a save button.

The page should include a section to reset the user's password with a save button. New password should be confirmed.

The page should include a link to a page to list locations and edit them. Stub this page for now, we will implement it later.